import { echo } from 'engine-test/helpers/echo';
import { module, test } from 'qunit';

module('Unit | Helper | echo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = echo([42]);
  assert.ok(result);
});
